This repository hosts our third project for PHY607, which focused on parameter estimation using Bayesian inference.

Usage instructions:
main-script --plot [mass_scatter, traces, full_signal, corner, emcee, chains] --nwalkers INT --nsteps INT --proposal_mode [newton, rw, mix]
